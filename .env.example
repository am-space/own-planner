# Local Docker deployment settings. Copy to .env and keep real secrets out of git.
OWNPLANNER_PORT=8080
# Used by the interactive deployment; live-AI tests require an exported GEMINI_API_KEY.
GEMINI_API_KEY=

